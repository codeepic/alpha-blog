== README

This is simple blog engine written in Ruby on Rails.

To run the app:
* clone it by typing <tt>git clone https://github.com/codeepic/alpha-blog.git</tt> in the command line
* run <tt>bundle install</tt>
* start the server - type in <tt>rails server</tt> or just <tt>rails s</tt> in your command line.
